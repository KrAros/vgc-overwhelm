# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Configurazione personale di Claude Code: permessi e preferenze di chi
# sviluppa, non del progetto. Le skill in .claude/skills/ restano tracciate.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Documenti di lavoro interni — scelta di Simone, 21 agosto 2026.
#
# CLAUDE.md e il piano sono il diario del risanamento: ogni criterio
# sbagliato, ogni punto cieco, ogni errore scritto per esteso. Servono a chi
# lavora al progetto, non a chi lo visita, e restano sul disco esattamente
# dove erano — sono solo usciti dal repository pubblico.
#
# ATTENZIONE: questo li toglie da qui in avanti. Sono ancora leggibili nei
# commit fino al 21 agosto 2026, perche' `git rm` non riscrive la storia.
# Chiuderlo anche all'indietro richiede un filter-repo e un force-push.
CLAUDE.md
docs/piano-risanamento.md
docs/analisi-critica.md

# Anche le skill: descrivono il metodo di lavoro, cioe' la stessa materia
# dei tre file qui sopra. Restano sul disco e continuano a funzionare.
.claude/
